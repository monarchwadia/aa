package main

// main is wired in the `session-manager-and-cli` workstream (wave 4).
// Until then this is a placeholder so the package builds while contract
// files and sub-workstream code are developed.
func main() {
	panic("unimplemented — see workstream `session-manager-and-cli` in docs/architecture/aa.md § Workstreams")
}
